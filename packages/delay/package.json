{
  "name": "@highoutput/delay",
  "version": "0.2.1",
  "main": "build/index.js",
  "scripts": {
    "clean": "rimraf build/",
    "build": "npm run clean && tsc --project tsconfig.json",
    "test": "mocha \"./test/**/*.spec.ts\"",
    "preversion": "npm run build && git add -A && (git commit -m \"build(logger): generate distribution files\" || true)"
  },
  "author": "HighOutput Ventures",
  "license": "MIT",
  "dependencies": {
    "@types/node": "^14.11.1",
    "ms": "^2.1.2"
  },
  "devDependencies": {
    "@types/mocha": "^8.0.3",
    "@types/ms": "^0.7.31",
    "mocha": "^8.1.3",
    "rimraf": "^3.0.0",
    "ts-node": "^9.0.0",
    "typescript": "^4.0.3"
  },
  "publishConfig": {
    "access": "public"
  }
}
