import { Meta, Preview } from '@storybook/addon-docs';

<Meta
  title="Releases/v0.4.2"
  parameters={{
    previewTabs: {
      canvas: { hidden: true },
    },
  }}
/>

# v0.4.2

v0.4.2 contains API breaking changes and new features.

## 🚀 Features

- added **For** utility component
- added **Mount** utility component
- imported **Helvetica Neue** as the default font style
- added `heading-web-1` in text size
- added `heading-web-2` in text size
- added `heading-web-3` in text size
- added `heading-web-4` in text size
- added `heading-web-5` in text size
- added `heading-web-6` in text size
- added `heading-mobile-1` in text size
- added `heading-mobile-2` in text size
- added `heading-mobile-3` in text size
- added `heading-mobile-4` in text size
- added `heading-mobile-5` in text size
- added `heading-mobile-6` in text size
- added `paragraph-xxl-default` in text size
- added `paragraph-xl-default` in text size
- added `paragraph-lg-default` in text size
- added `paragraph-md-default` in text size
- added `paragraph-sm-default` in text size
- added `paragraph-xs-default` in text size
- added `paragraph-xxs-default` in text size
- added `paragraph-xxl-italic` in text size
- added `paragraph-xl-italic` in text size
- added `paragraph-lg-italic` in text size
- added `paragraph-md-italic` in text size
- added `paragraph-sm-italic` in text size
- added `paragraph-xs-italic` in text size
- added `paragraph-xxs-italic` in text size
- added `paragraph-xxl-bold` in text size
- added `paragraph-xl-bold` in text size
- added `paragraph-lg-bold` in text size
- added `paragraph-md-bold` in text size
- added `paragraph-sm-bold` in text size
- added `paragraph-xs-bold` in text size
- added `paragraph-xxs-bold` in text size
- added `label-xl-default` in text size
- added `label-lg-default` in text size
- added `label-md-default` in text size
- added `label-sm-default` in text size
- added `label-xs-default` in text size
- added `label-xxs-default` in text size
- added `label-xl-italic` in text size
- added `label-lg-italic` in text size
- added `label-md-italic` in text size
- added `label-sm-italic` in text size
- added `label-xs-italic` in text size
- added `label-xxs-italic` in text size
- added `label-xl-medium` in text size
- added `label-lg-medium` in text size
- added `label-md-medium` in text size
- added `label-sm-medium` in text size
- added `label-xs-medium` in text size
- added `label-xxs-medium` in text size
- added `label-xl-bold` in text size
- added `label-lg-bold` in text size
- added `label-md-bold` in text size
- added `label-sm-bold` in text size
- added `label-xs-bold` in text size
- added `label-xxs-bold` in text size
- added `uppercase-xl-default` in text size
- added `uppercase-lg-default` in text size
- added `uppercase-md-default` in text size
- added `uppercase-sm-default` in text size
- added `uppercase-xs-default` in text size
- added `uppercase-xxs-default` in text size
- added `uppercase-xl-italic` in text size
- added `uppercase-lg-italic` in text size
- added `uppercase-md-italic` in text size
- added `uppercase-sm-italic` in text size
- added `uppercase-xs-italic` in text size
- added `uppercase-xxs-italic` in text size
- added `uppercase-xl-medium` in text size
- added `uppercase-lg-medium` in text size
- added `uppercase-md-medium` in text size
- added `uppercase-sm-medium` in text size
- added `uppercase-xs-medium` in text size
- added `uppercase-xxs-medium` in text size
- added `uppercase-xl-bold` in text size
- added `uppercase-lg-bold` in text size
- added `uppercase-md-bold` in text size
- added `uppercase-sm-bold` in text size
- added `uppercase-xs-bold` in text size
- added `uppercase-xxs-bold` in text size
- added `monospace-xl-default` in text size
- added `monospace-lg-default` in text size
- added `monospace-md-default` in text size
- added `monospace-sm-default` in text size
- added `monospace-xs-default` in text size
- added `monospace-xxs-default` in text size
- added `monospace-xl-italic` in text size
- added `monospace-lg-italic` in text size
- added `monospace-md-italic` in text size
- added `monospace-sm-italic` in text size
- added `monospace-xs-italic` in text size
- added `monospace-xxs-italic` in text size
- added `monospace-xl-medium` in text size
- added `monospace-lg-medium` in text size
- added `monospace-md-medium` in text size
- added `monospace-sm-medium` in text size
- added `monospace-xs-medium` in text size
- added `monospace-xxs-medium` in text size
- added `monospace-xl-bold` in text size
- added `monospace-lg-bold` in text size
- added `monospace-md-bold` in text size
- added `monospace-sm-bold` in text size
- added `monospace-xs-bold` in text size
- added `monospace-xxs-bold` in text size
- added `button-default` in text size
- added `button-uppercase` in text size

## ❗ Refactor/ Breaking Changes

- deprecate `Inter` as default font style
- deprecate `heading-1` in text size
- deprecate `heading-2` in text size
- deprecate `heading-3` in text size
- deprecate `heading-4` in text size
- deprecate `heading-5` in text size
- deprecate `heading-6` in text size
- deprecate `title-16-semibold` in text size
- deprecate `title-16-md` in text size
- deprecate `title-16-regular` in text size
- deprecate `title-14-semibold` in text size
- deprecate `title-14-md` in text size
- deprecate `title-14-regular` in text size
- deprecate `body-16-semibold` in text size
- deprecate `body-16-md` in text size
- deprecate `body-16-regular` in text size
- deprecate `body-14-semibold` in text size
- deprecate `body-14-md` in text size
- deprecate `body-14-regular` in text size
- deprecate `body-12-semibold` in text size
- deprecate `body-12-md` in text size
- deprecate `body-12-regular` in text size
- deprecate `body-10-regular` in text size
- deprecate `button-16-semibold` in text size
- deprecate `button-16-md` in text size
- deprecate `button-16-regular` in text size
- deprecate `button-14-semibold` in text size
- deprecate `button-14-md` in text size
- deprecate `button-14-regular` in text size
- deprecate `caption-14-semibold` in text size
- deprecate `caption-14-md` in text size
- deprecate `caption-14-regular` in text size
- deprecate `caption-12-semibold` in text size
- deprecate `caption-12-md` in text size
- deprecate `caption-12-regular` in text size
- deprecate `caption-10-semibold` in text size
- deprecate `caption-10-md` in text size
- deprecate `caption-10-regular` in text size
- deprecate `text-5xl` in text size
- deprecate `text-3xl` in text size
- deprecate `text-base` in text size

## 🧹 Chores:

- added typography page
