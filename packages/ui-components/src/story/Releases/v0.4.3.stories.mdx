import { Meta, Preview } from '@storybook/addon-docs';

<Meta
  title="Releases/v0.4.3"
  parameters={{
    previewTabs: {
      canvas: { hidden: true },
    },
  }}
/>

# v0.4.3

v0.4.3 contains API breaking changes and new features.

## 🚀 Features

- added `alpha.white` in color system
- added `alpha.black` in color system
- added `canvas.light` in color system
- added `canvas.dark` in color system
- added `brand.primary` in color system
- added `brand.secondary` in color system
- added `brand.tertiary` in color system
- added `neutrals` in color system
- added `interface.error` in color system
- added `interface.success` in color system
- added `interface.warning` in color system

## ❗ Refactor/ Breaking Changes

- deprecate `primary` in color system
- deprecate `secondary` in color system
- deprecate `tertiary` in color system
- deprecate `greenLight` in color system
- deprecate `limeLight` in color system
- deprecate `yellowLight` in color system
- deprecate `orangeLight` in color system
- deprecate `redLight` in color system
- deprecate `textColor` in color system
- deprecate `skyLight` in color system
- deprecate `roseRed` in color system
- deprecate `sunglow` in color system
- deprecate `amberDark` in color system
- deprecate `whiteDark` in color system
- deprecate `rose` in color system
- deprecate `amber` in color system
- deprecate `teal` in color system
- deprecate `purple` in color system
- deprecate `indigo` in color system
- deprecate `magenta` in color system
- deprecate `secondary` theme variant in **Switch** component
- deprecate `orange` theme variant in **Switch** component
- deprecate `tertiatry-error` theme variant in **Button** component
- deprecate `primary-circle` theme variant in **Button** component
- deprecate `solid-gray` theme variant in **Button** component
- deprecate `upload-circle` theme variant in **Button** component
- deprecate `ghost` theme variant in **Button** component
- deprecate `feed-card-button` theme variant in **Button** component
- deprecate `outline-follow` theme variant in **Button** component
- deprecate `outline-unfollow` theme variant in **Button** component
- deprecate `outline-icon-button` theme variant in **Button** component
- deprecate `link` theme variant in **Button** component

## 🧹 Chores:

- added color document page
- added color system for sample color and variable token page
