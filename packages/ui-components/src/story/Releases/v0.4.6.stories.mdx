import { Meta, Preview } from '@storybook/addon-docs';

<Meta
  title="Releases/v0.4.6 - Latest"
  parameters={{
    previewTabs: {
      canvas: { hidden: true },
    },
  }}
/>

# v0.4.5

v0.4.5 contains API breaking changes and new features.

## 🧹 Chores:

- expose `InputField` component and `InputFieldProps` interface
- expose `SelectField` component and `SelectFieldProps` interface
- expose `PasswordInputField` component and `PasswordInputFieldProps` interface
- expose `TextAreaField` component and `TextAreaFieldProps` interface
- expose `FormContainer` component and `FormContainerProps` interface
- expose `ArrayField` component and `ArrayFieldProps` interface
